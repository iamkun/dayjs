name: Lint & Unit Test
on:
  push:
    branches: [master, dev]
  pull_request:
    branches: [dev]

jobs:
  check:
    uses: ./.github/workflows/lint-test.yml
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
